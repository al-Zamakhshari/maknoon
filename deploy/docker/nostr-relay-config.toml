# Minimal nostr-rs-relay configuration for mission testing
[info]
relay_url = "ws://nostr-relay:8080"
name = "maknoon-test-relay"
description = "Ephemeral test relay for mission verification"
contact = "test@maknoon.local"

[database]
data_directory = "/tmp/nostr-data"

[network]
address = "0.0.0.0"
port = 8080
